const base = 10;
const altura = 5;
const area = base * altura;
const perimetro = 2 * (base + altura);
console.log(`el area del rectangulo es ${area} cm²`);
console.log(`el perimetro del rectangulo es ${perimetro} cm`)